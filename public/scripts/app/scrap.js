// CSS Menu JS operations for AJAXY goodness.

/*      $('#genre').live('click', function (e) {
        if (on) {
        e.stopPropagation()
        e.preventDefault()
        $('nav ul ul').css({'display': 'none'})
        var url = event.target.attributes[0].value;
        getData(url)
        }
      })

      $('#genre').live('mouseover', function () {
        $('nav ul ul').css({'display': 'block'})
      })

      $('#genre').live('mouseout', function () {
        $('nav ul ul').css({'display': 'none'})
      })
    })
*/
